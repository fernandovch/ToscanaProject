@model List<ToscanaWebApp.Models.Producto>

<div class="container">
    <div class="row row-offcanvas row-offcanvas-right">

        <div class="col-xs-12 col-sm-9">
            <p class="pull-right visible-xs">
                <button type="button" class="btn btn-primary btn-xs" data-toggle="offcanvas">Toggle nav</button>
            </p>
            <div class="row">

                <h1>Hello, FErnando!</h1>
                <p>Direccion</p><p>TElefonos</p>


            </div>
            <div class="row">

                @foreach (var item in Model)
                {
                    <div class="col-xs-6 col-lg-4">
                        <div class="form-group">

                            <label for="pedidoProducto">
                                @Html.DisplayFor(modelItem => item.Nombre)
                            </label>
                            <p>@Html.DisplayFor(modelItem => item.Descripcion)</p>



                            @Html.TextBox("Cantidad", "Ingrese cantidad", new { style = "color:red" })

                            <a class="btn btn-default" asp-action="Agrega" asp-route-idProducto="@item.Id"
                               asp-route-Cantidad="@item.Cantidad">Agregar al pedido</a>

                            @Html.ActionLink("Server-side Link", "Agrega", "Catalogo",
                     new { id = @item.Id, Cantidad = document.getElementById("Cantidad") })
                        </div>
                    </div>
                }


            </div><!--/row-->
        </div><!--/.col-xs-12.col-sm-9-->

    </div><!--/row-->
</div><!--/.container-->
