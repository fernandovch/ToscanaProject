@model List<ToscanaWebApp.Models.Producto>

<div class="container">
    <div class="row row-offcanvas row-offcanvas-right">

        <div class="col-xs-12 col-sm-9">
            <p class="pull-right visible-xs">
                <button type="button" class="btn btn-primary btn-xs" data-toggle="offcanvas">Toggle nav</button>
            </p>
            <div class="row">

                <h1>Hello, FErnando!</h1>
                <p>Direccion</p><p>TElefonos</p>
 

            </div>
            <div class="row">

                @foreach (var item in Model)
                {
                    <div class="col-xs-6 col-lg-4">
                        <div class="form-group">
                            <label for="pedidoProducto">
                                @Html.DisplayFor(modelItem => item.Nombre)
                            </label>
                            <p>@Html.DisplayFor(modelItem => item.Descripcion)</p>
                            <input type="text" class="form-control" id="pedidoProducto">
                            <a class="btn btn-default" asp-action="Agrega">Agregar al pedido</a>
                            <a class="btn btn-default" asp-controller="Catalogo" asp-action="Valida" asp-route-id="10">Valida</a>
                        </div>
                    </div>
                }
            </div><!--/row-->
        </div><!--/.col-xs-12.col-sm-9-->
     
    </div><!--/row-->
</div><!--/.container-->
